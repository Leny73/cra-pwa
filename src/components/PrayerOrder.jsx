import { Container } from 'react-bootstrap';

const PrayerOrder = () => {
    return <Container className="mt-2" style={{minHeight: 'calc(100vh - 180px)'}} fluid > <div>Test prayer order</div>
    </Container>
}

export default PrayerOrder;