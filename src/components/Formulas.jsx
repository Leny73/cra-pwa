import { Container } from 'react-bootstrap';

const Formulas = () => {
    return <Container className="mt-2" style={{minHeight: 'calc(100vh - 180px)'}} fluid >
        <div>Test Formulas</div>
    </Container>
}

export default Formulas;